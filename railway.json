{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python test_startup.py && python manage.py migrate && gunicorn mangoAPI.wsgi:application --bind 0.0.0.0:$PORT --workers 1 --threads 4 --timeout 300 --log-level debug --access-logfile - --error-logfile -",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
